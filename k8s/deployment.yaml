apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: willchrist/frontend-service:648c74c
          ports:
            - containerPort: 8000
          resources:
            requests:
              memory: "256Mi" # minimum memory guaranteed
              cpu: "250m" # minimum CPU guaranteed
            limits:
              memory: "512Mi" # maximum memory allowed
              cpu: "500m"
          envFrom:
            - configMapRef:
                name: ecommerce-config
